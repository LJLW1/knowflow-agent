# 三分钟演示视频脚本

**0:00–0:25** 展示 README 和架构图，说明 KnowFlow 是独立 Hermes 扩展，不修改上游 Agent Loop。

**0:25–1:00** 用 Streamlit 上传 Markdown、TXT、DOCX、PDF，展示重复上传跳过和索引版本。

**1:00–1:40** 提问发布要求，展开文件名、章节、页码和证据片段；再问知识库不存在的问题，展示拒答。未配置 Key 时先解释明确错误，再使用本人配置的演示模型。

**1:40–2:15** 在 Hermes 中调用 `knowledge_search`、`knowledge_answer`，展示只读 GitHub MCP；尝试写工具或访问其他仓库时由前置策略阻断。

**2:15–2:40** 展示三个 child 的有限流程、部分失败降级、日报幂等和 trace ID 日志。

**2:40–3:00** 打开评测报告和 GitHub Actions，强调 56 条数据集、真实本地检索数字，以及仍为“待测量”的云模型指标。
