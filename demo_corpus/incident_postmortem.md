# 2026-07-12 缓存配置事故复盘

## 影响

错误的缓存 TTL 配置在 14:05 发布，导致 18% 的查询返回过期状态。故障持续 27 分钟，没有造成数据丢失。

## 处置时间线

14:08 监控触发状态不一致告警。14:12 当班工程师确认新配置是共同变化点。14:17 团队暂停流量并回滚配置。14:24 错误率恢复正常，14:32 完成缓存清理。

## 根因

配置审查只校验了格式，没有验证 TTL 的业务上限；预发布环境也没有覆盖过期状态场景。

## 改进项

为 TTL 增加不超过 300 秒的规则校验；在发布流水线加入过期状态回归测试；配置变更必须由两人复核。
